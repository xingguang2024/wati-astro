# DefaultRepositoryTemplate
Include company wide standard and configurations to avoid boilerplate work when creating new projects.

# Sample GitHub CLI command for creating new repository  
```
gh repo create ClareAI/{your_repo_name} --clone --internal --template ClareAI/default-repository-template
```
