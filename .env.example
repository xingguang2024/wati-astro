# JWT Secret - Generate a secure random string (min 32 characters)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this-in-production

# AI Gateway API Key (for existing PlateEditor AI features)
AI_GATEWAY_API_KEY=

# UploadThing Token (for existing file upload features)
# Note: The application now uses Cloudflare R2 for file uploads by default
# R2 is configured via wrangler.jsonc binding, no additional env vars needed
UPLOADTHING_TOKEN=

# Cloudflare R2 Storage
# R2 bucket is configured in wrangler.jsonc under r2_buckets binding
# The application uses the "R2" binding to upload files to "wati-astro-uploads" bucket
# For local development, wrangler provides a local R2 mock with --local flag
# File access URLs: Files are stored at /uploads/{userId}/{year}/{month}/{uuid}.{ext}
